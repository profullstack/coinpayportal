<?php
/** @var array  $invoice       FOSSBilling invoice array */
/** @var string $checkout_url  CoinPayPortal checkout URL */
/** @var string $display_name  Gateway display name */
$invoiceNr = htmlspecialchars($invoice['serie_nr'] ?? (string)($invoice['id'] ?? ''));
$total     = htmlspecialchars(number_format((float)($invoice['total'] ?? 0), 2));
$currency  = htmlspecialchars($invoice['currency'] ?? 'USD');
$name      = htmlspecialchars($display_name);
$url       = htmlspecialchars($checkout_url);
?>
<div class="coinpayportal-checkout" style="text-align:center;padding:24px 16px;font-family:sans-serif;">
  <p style="font-size:15px;margin-bottom:4px;">
    Invoice #<?= $invoiceNr ?> &mdash; <strong><?= $total ?> <?= $currency ?></strong>
  </p>
  <p style="font-size:13px;color:#555;margin-bottom:20px;">
    You will be redirected to <?= $name ?> to complete your crypto payment.
    Your invoice will update automatically after the payment is confirmed.
  </p>
  <form method="get" action="<?= $url ?>">
    <button type="submit"
      style="background:#7c3aed;color:#fff;border:none;border-radius:6px;padding:12px 28px;font-size:15px;cursor:pointer;">
      Pay with <?= $name ?>
    </button>
  </form>
</div>
